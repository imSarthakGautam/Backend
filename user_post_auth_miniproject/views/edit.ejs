<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 text-white">

    <div class="container mx-auto p-8">
        <!-- Welcome Message -->
        <h1 class="text-3xl font-bold text-yellow-400 mb-8"> <%= post.user.name  %>!</h1>

        <!-- Edit Post Form -->
        <div class="bg-gray-800 p-6 rounded-md mb-10 shadow-md">
            <h2 class="text-2xl font-semibold text-yellow-400 mb-4">Edit Post</h2>
            <form action="/edit-post/<%= post._id %>" method="POST" class="space-y-4">
                <textarea name="postContent" rows="3" placeholder="What's on your mind?" class="w-full p-3 rounded-md bg-gray-700 text-white focus:outline-none focus:ring-2 focus:ring-yellow-400">
                    <%= post.content  %>
            </textarea>
                <button type="submit" class="bg-yellow-500 text-black font-semibold px-4 py-2 rounded-md hover:bg-yellow-400 transition duration-300">Update</button>
            </form>
        </div>
       
    </div>

</body>
</html>
