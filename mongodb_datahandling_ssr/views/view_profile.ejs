<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Saved Profiles</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">

  <div class="container mx-auto p-8">
    <h1 class="text-3xl font-bold text-center mb-8 text-yellow-400">Saved Profiles</h1>

    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
     <% if (all_users.length > 0) { %>
      <% all_users.forEach((user) => { %>
        <div class="bg-zinc-800 shadow-lg rounded-md p-6 flex flex-col items-center overflow-hidden">
            <img src="<%= user.image_url %>" alt="Profile Image" class="w-full h-full object-cover rounded-full mb-4">
            <h2 class="text-xl font-semibold text-yellow-400 mb-2"><%= user.name %></h2>
            <p class="text-zinc-400"><%= user.email %></p>
            <div class="mt-4 flex space-x-4">
              <!-- Edit Button -->
              <a href="/edit/<%= user._id %>" class="bg-yellow-500 text-white py-1 px-3 rounded hover:bg-yellow-600">Edit</a>
              <a href="/delete/<%= user._id %>" class="bg-red-500 text-white py-1 px-3 rounded hover:bg-red-600">Delete</a>
              <!-- Delete Button -->
             
            </div>
        </div>
      <% }) %>
      <% }else{ %>
        <div> No users yet</div>
        <% } %>
    </div>

    <a href="/" class="block text-center mt-8 text-yellow-400 hover:underline">Back to Form</a>
  </div>

</body>
</html>
